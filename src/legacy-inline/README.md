# Legacy Inline Scripts

These files were extracted from inline `<script>` blocks in `org-dashboard.html`.

The file names include the original script order. Preserve this order when wiring the modular version.
