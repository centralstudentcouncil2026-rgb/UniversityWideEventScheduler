# Legacy Bundle

These files were extracted from the embedded `moduleSources` object inside `org-dashboard.html`.

During the first Codex pass, treat these as the readable version of the current working app. Do not delete or rewrite them until the modular app is verified.
