# Legacy External Scripts

These files came directly from the uploaded ZIP outside the main HTML file.

They may overlap with inline versions. When in doubt, compare with `docs/EXTRACTION-REPORT.json` and the original compacted dashboard.
