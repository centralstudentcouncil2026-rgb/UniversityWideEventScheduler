// Shared state placeholder.
// Codex: centralize dashboard state here after verifying the extracted legacy bundle works.

export const ORG_DASHBOARD_STATE_MIGRATION_NOTE =
  'State currently still lives inside legacy-bundle/script.js and related guards.';
